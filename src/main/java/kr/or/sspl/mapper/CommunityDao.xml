<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.sspl.dao.CommunityDao">
	<select id="communityCount" resultType="int">
		select count(*)
    		from community
	</select>
	
	<select id="list" parameterType ="HashMap" resultType="kr.or.sspl.dto.CommunityDto">
		select comm_seq,user_id,comm_title,comm_content,comm_writen_date,comm_category,comm_view_count
		from (select rownum as rn, comm_seq,user_id,comm_title,comm_content,comm_writen_date,comm_category,comm_view_count
		from community
		order by rn asc)
		where rn between #{start} and #{end}
	</select>


</mapper>